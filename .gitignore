# Los .sql siembran dim_mrr (MRR por cliente) y otras dimensiones: nunca al repo.
# GitHub Pages sirve todo lo que hay en la rama, aunque el repo sea privado.
*.sql
# Variante completa del mapa (con la foto incrustada): solo para uso local.
_local/
mapa_tickets_v*.html
# Carpeta donde la app de Claude deja los archivos enviados al chat (capturas, etc.): nunca al repo.
Claude outputs/
